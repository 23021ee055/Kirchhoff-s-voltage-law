import math

print("LC RESONANT FREQUENCY")

L = float(input("Enter inductance (H): "))
C = float(input("Enter capacitance (F): "))

f = 1 / (2 * math.pi * math.sqrt(L * C))

print("Resonant Frequency =", f, "Hz")

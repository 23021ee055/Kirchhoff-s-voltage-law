output = float(input("Enter output power (W): "))
input_power = float(input("Enter input power (W): "))

efficiency = (output / input_power) * 100

print(f"Transformer Efficiency = {efficiency:.2f}%")
